// import {StatePropsType, ActionTypes, DialogsPagePropsType} from "./State";

// const SEND_MESSAGE = "SEND-MESSAGE"
// const CHANGE_MESSAGE = "CHANGE-MESSAGE"

// export const profileReducer = (state: StatePropsType, action: ActionTypes) => {
//     if (action.type === SEND_MESSAGE) {
//         let newMessage = { id: 6, textMessage: state.newMessageText };
//         state.messages.push(newMessage);
//         state.newMessageText = "";

//     } else if (action.type === CHANGE_MESSAGE) {
//         state.newMessageText = action.messageText;
//     }

//     return state;
// }
